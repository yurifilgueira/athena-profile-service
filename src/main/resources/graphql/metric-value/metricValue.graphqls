type MetricValue {
    value: Float!,
    description: String!,
    metricType: MetricType!
}

input MetricValueInput {
    value: Float!,
    description: String!,
    metricType: MetricType!
}

enum MetricType {
    COMMIT_PER_DEVELOPER,
    LOC_PER_DEVELOPER
}